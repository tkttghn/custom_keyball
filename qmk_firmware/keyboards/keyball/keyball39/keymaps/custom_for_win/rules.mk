# 定義すると、config.hのLED設定部分が有効になる
#RGBLIGHT_ENABLE = yes

# 定義すると、keymap.cのOLED設定部分が有効になる
OLED_ENABLE = yes

# VIA(REMAP)の設定に必要な定義
VIA_ENABLE = yes

# share the grave key (` and ~) with Escape.
# GRAVE_ESC_ENABLE = no